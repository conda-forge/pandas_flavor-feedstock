{% set version = "0.8.1" %}

package:
  name: pandas-flavor
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/p/pandas_flavor/pandas_flavor-{{ version }}.tar.gz
  sha256: 255fa5851833ee0132c4fdd6c1565ec1e938a8c2671c37e408006da6b2bdc366

build:
  number: 0
  noarch: python

outputs:
  - name: pandas-flavor
    script: build_base.sh
    requirements:
      host:
        - python {{ python_min }}
        - setuptools
        - pip
      run:
        - python >={{ python_min }}
        - pandas >=0.23
        - xarray
    test:
      imports:
        - pandas_flavor
      commands:
        - pip check
      requires:
        - pip

  - name: pandas_flavor
    build:
      noarch: generic
    requirements:
      run:
        - {{ pin_subpackage('pandas-flavor', max_pin="x.x.x") }}
    test:
      imports:
        - pandas_flavor

about:
  home: https://github.com/Zsailer/pandas_flavor
  summary: The easy way to write your own Pandas flavor.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ocefpaf
    - zsailer
